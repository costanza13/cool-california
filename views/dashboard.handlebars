<section>
  <h2>Your Dashboard</h2>
  <p>Hi {{username}}. Welcome to your account dashboard! Here you can manage your existing posts or add a new one.</p>
  <p>You've written {{posts.length}} {{format_plural 'post' posts.length}}, so far.</p>
  <a href="/dashboard/create">Add a new post</a>
</section>

{{#if posts.length}}
<section>
  <h3>Your Posts</h3>
  <ul>
    {{#each posts as |post|}}
    <li>
      {{> post-info post}}
      <a href="/dashboard/edit/{{post.id}}">Edit post</a>
    </li>
    {{/each}}
  </ul>
</section>
{{/if}}