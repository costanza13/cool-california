<article>
  <p>Post ID: {{post.id}}</p>
  <p>Author: <a href="/user/{{post.user.post_author_id}}">{{post.user.post_author}}</a></p>
  <p><img src="{{post.image_url}}" /></p>
  <p>Title: <a href="/post/{{post.id}}">{{post.title}}</a></p>
  <p>Description: {{post.description}}</p>
  <p>Image URL: [{{post.image_url}}]</p>
  <p>Tags:
    {{#each post.tags}}
    [<a href="/tag/{{tag_id}}">{{tag_name}}</a>]
    {{/each}}
  </p>
  <p>Created at: {{post.created_at}}</p>
  <p id="post-{{id}}-vote">
    <span class="vote-like"><input type="radio" class="vote-input" name="vote-{{post.id}}" id="like-button-{{post.id}}"
        value="like" {{#if post.liked}} checked="checked" {{/if}} data-post-id="{{post.id}}"> <label
        for="like-button-{{post.id}}">Like (<span class="like-count">{{post.likes}}</span>)</label></span>
    <span class="vote-dislike"><input type="radio" class="vote-input" name="vote-{{post.id}}"
        id="dislike-button-{{post.id}}" value="dislike" {{#if post.unliked}} checked="checked" {{/if}}
        data-post-id="{{post.id}}"> <label for="dislike-button-{{post.id}}">Dislike (<span
          class="dislike-count">{{post.dislikes}}</span>)</label></span>
    <input type="radio" class="vote-input" name="vote-{{post.id}}" id="novote-button-{{post.id}}" value="novote" {{#if
      post.novote}} checked="checked" {{/if}} data-post-id="{{post.id}}"> no vote
  </p>
  <p><a href="/post/{{post.id}}">{{post.comment_count}} comments</a></p>
</article>
<script src="/javascript/vote.js"></script>